const crypto = require('crypto');

// Function to generate strong random bytes
const generateStrongRandomBytes = (length) => {
    return crypto.randomBytes(length).toString('hex');
};

module.exports = { generateStrongRandomBytes };
